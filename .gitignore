# Node
node_modules/

# Development
_test/
temp/
# Arbiter runtime state (bind-mounted as the container's /app/log). Holds the
# durable mailbox/pending-job snapshots, the log, AND the Host's private
# federation keypair - must never be committed.
/.cursor/
/.swc/
/.vscode-server/
/.vscode/
/coverage/
/ssl/
/volumes/
/.env*
/compose*.yml
/next-env.d.ts
/.playwright-mcp/
/.claude/scheduled_tasks.lock
debug*.log
kubeconfig*

#### OS
Thumbs.db
.DS_Store
._.DS_Store
plans/*.cycle.json
